<style>
#site-header {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 999;
    background: transparent;
    border-bottom: 1px solid transparent;
    transition: background 0.3s ease, border-color 0.3s ease, backdrop-filter 0.3s ease;
}

#site-header.scrolled {
    background: rgba(255,255,255,0.82);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border-bottom: 1px solid #e5e5e5;
}

#site-header .header-inner {
    max-width: calc(1200px + 20px * 2);
    margin: 0 auto;
    padding: 18px 20px;
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    column-gap: 20px;
    box-sizing: border-box;
}

#site-header .nav {
    justify-self: start;
    display: flex;
    gap: 28px;
    align-items: center;
}

#site-header .nav a {
    text-decoration: none;
    color: #8b9098;
    font-size: 14px;
    font-weight: 500;
    letter-spacing: 0.05em;
    transition: color 0.2s ease;
}

#site-header .nav a:hover {
    color: #2478C4;
}

#site-header .logo {
    justify-self: center;
    display: flex;
    align-items: center;
    justify-content: center;
}

#site-header .logo img {
    height: 44px;
    width: auto;
    display: block;
}

#site-header .right-block {
    justify-self: end;
    min-width: 1px;
}

body {
    padding-top: 84px;
}

@media (max-width: 768px) {
    #site-header .header-inner {
        grid-template-columns: 1fr;
        grid-template-rows: auto auto;
        row-gap: 10px;
        padding: 14px 16px;
    }

    #site-header .logo {
        grid-row: 1;
    }

    #site-header .logo img {
        height: 38px;
    }

    #site-header .nav {
        grid-row: 2;
        justify-self: center;
        gap: 20px;
        flex-wrap: wrap;
    }

    #site-header .nav a {
        font-size: 13px;
    }

    #site-header .right-block {
        display: none;
    }

    body {
        padding-top: 92px;
    }
}
</style>

<header id="site-header">
    <div class="header-inner">
        <nav class="nav">
            <a href="#about" class="js-scroll-link">О компании</a>
            <a href="#contacts" class="js-scroll-link">Контакты</a>
        </nav>

<div class="logo">
    <a href="#" class="js-scroll-top">
        <img src="http://91.186.208.56/img/logo_delta.png" alt="ЦП Дельта">
    </a>
</div>

        <div class="right-block"></div>
    </div>
</header>

<script>
(function () {
    const header = document.getElementById('site-header');
    if (!header) return;

    function updateHeader() {
        if (window.scrollY > 30) {
            header.classList.add('scrolled');
        } else {
            header.classList.remove('scrolled');
        }
    }

    window.addEventListener('scroll', updateHeader);
    updateHeader();

    document.querySelectorAll('.js-scroll-link').forEach(function(link) {
        link.addEventListener('click', function(e) {
            e.preventDefault();

            const targetId = this.getAttribute('href');
            const target = document.querySelector(targetId);
            const headerHeight = header.offsetHeight || 0;

            if (target) {
                const targetPosition =
                    target.getBoundingClientRect().top +
                    window.pageYOffset -
                    headerHeight -
                    16;

                window.scrollTo({
                    top: targetPosition,
                    behavior: 'smooth'
                });
            }
        });
    });
document.querySelectorAll('.js-scroll-top').forEach(function(link) {
    link.addEventListener('click', function(e) {
        e.preventDefault();

        window.scrollTo({
            top: 0,
            behavior: 'smooth'
        });
    });
});
})();
</script>